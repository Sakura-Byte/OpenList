# OpenList 元信息隐藏

本词汇表描述当前元信息隐藏与路径浏览的术语。

## Language

**元信息**：绑定到一个 OpenList 路径的设置，包括隐藏规则、可读用户、目录密码和目录说明。

**隐藏规则**：按名称选择需要对当前用户隐藏的文件或目录的规则，其作用范围由现有元信息配置决定。
_Avoid_：目录密码

**查看隐藏权限**：允许用户查看被隐藏规则命中的条目的用户权限。它与密码、可读用户等访问条件分别生效。

**隐藏路径**：按照现有隐藏规则，当前用户不应看到的路径。在目录浏览和文件详情中，它的公开表现与不存在的路径相同。

**路径未找到**：目录浏览或文件详情没有可向当前用户展示的目标对象。该结果不透露目标是实际缺失还是被隐藏。

**有效下载签名**：授予持有人相应下载能力的有效凭据。用户接受这种授权方式，本次调整聚焦普通目录浏览和文件详情。
